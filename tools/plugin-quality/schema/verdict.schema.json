{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Plugin-quality LLM-judge verdict",
  "description": "The JSON contract the judged model must emit. The harness — not the model — decides pass/fail by comparing each score against the dimension floor in rubrics.py.",
  "type": "object",
  "required": ["dimensions"],
  "additionalProperties": true,
  "properties": {
    "dimensions": {
      "type": "object",
      "description": "Map of dimension id (J1..J11) to its score.",
      "minProperties": 1,
      "additionalProperties": {
        "type": "object",
        "required": ["score", "evidence"],
        "additionalProperties": true,
        "properties": {
          "score": {
            "type": "integer",
            "minimum": 1,
            "maximum": 5,
            "description": "1 = fails the dimension badly, 5 = exemplary."
          },
          "evidence": {
            "type": "string",
            "minLength": 1,
            "description": "Concrete quote or specific reason for the score. No vague praise."
          }
        }
      }
    },
    "overall_notes": {
      "type": "string",
      "description": "Optional one-paragraph summary."
    }
  }
}
