# LLM-as-judge report

- artifacts judged: 37
- blocking failures: 5
- advisory findings: 49
- errors: 0

## Blocking (critical, score <= block floor)
- `plugins/php-backend-sdlc/skills/ci-workflow/SKILL.md` J3 degrade-path-soundness = 2/5 — 'Repeat Steps 2-4 until it exits 0' defines no termination for the case where CI cannot be fixed — the loop is infinite with no ESCALATED/FAILED/STOP outcome, violating the no-infinite-loop requirement.
- `plugins/php-backend-sdlc/skills/clean-architecture-llm/SKILL.md` J3 degrade-path-soundness = 2/5 — Profile keys architecture.source_root, architecture.bounded_contexts, make.ci, and make.tests are consumed with no degrade path if absent; only architecture.shared_context gets a 'when one exists' conditional with a non-looping branch.
- `plugins/php-backend-sdlc/skills/code-organization/SKILL.md` J7 semantic-generalization = 2/5 — The profile-example block inside PHP Best Practices lists source-project entities verbatim: OAuthProviderCollectionFactory, SignInEventFactory, SessionRevocationEventFactory, TwoFactorEventFactory, RefreshTokenEventFactory, AuthSession, RecoveryCode — baking a user/OAuth service domain into what should be a generic skill.
- `plugins/php-backend-sdlc/skills/query-performance-analysis/SKILL.md` J3 degrade-path-soundness = 2/5 — No degrade path when persistence.engine is absent or unrecognized (the table silently has no else row); make.start missing produces no defined outcome; capabilities.load_testing gate says 'gated by' but never documents a SKIPPED terminal state.
- `plugins/php-backend-sdlc/skills/testing-workflow/SKILL.md` J3 degrade-path-soundness = 2/5 — Step 4 says 'Repeat Steps 2–4 until all tests pass' with no stopping condition, maximum iteration count, or escalation terminal — this loop can run forever when a failure is unresolvable; no BLOCKED/ESCALATED outcome is defined.

## Advisory (score below floor)
- `plugins/php-backend-sdlc/agents/fr-nfr-reviewer.md` J11 instruction-unambiguity = 3/5 — 'Re-running only to resolve an ambiguous or stale result' is undefined (no criteria for staleness/ambiguity), and 'same substance' as the dedup criterion for new findings lacks a precision anchor — two real multi-reading points.
- `plugins/php-backend-sdlc/agents/php-implementer.md` J11 instruction-unambiguity = 3/5 — 'docker compose exec php <command> for in-container commands the map does not cover' is ambiguous: a reader could interpret it as a bypass for null make targets (e.g. make.tests:null → docker compose exec php phpunit), conflicting with the degrade table that says null means skip, not substitute.
- `plugins/php-backend-sdlc/agents/pr-comment-resolver.md` J11 instruction-unambiguity = 3/5 — Degrade for no-reviewer-app runs ai-review-loop.sh --max-iterations 1 but the smoke prompt says agent 're-runs until PASS or counter spent' — it is unclear whether the agent loops calling the script or the script handles all iterations internally.
- `plugins/php-backend-sdlc/commands/sdlc-finish-pr.md` J11 instruction-unambiguity = 3/5 — Step 3 condition 'if review.coderabbit is true (or any AI reviewer app posts review comments)' is materially ambiguous: the first clause is a profile-config check, the second implies runtime inspection of PR comment authors with no method specified for detecting 'any AI reviewer app'.
- `plugins/php-backend-sdlc/commands/sdlc-implement.md` J11 instruction-unambiguity = 3/5 — Step 2 says 'run bmalph run --driver claude-code' while step 3 says 'inside the loop… fan out to parallel php-implementer subagents (Task tool)' — it is genuinely ambiguous whether bmalph orchestrates the dispatch or Claude Code manually fires Task calls alongside bmalph run.
- `plugins/php-backend-sdlc/commands/sdlc-issue.md` J4 exit-condition-fidelity = 3/5 — Loop re-checks 'URL resolves, ≥3 AC bullets present, marker label attached' but the FR-1 exit condition stated is 'GitHub issue URL exists with testable AC' — label requirement is verified but absent from the formal exit condition, creating drift.
- `plugins/php-backend-sdlc/commands/sdlc-issue.md` J11 instruction-unambiguity = 3/5 — 'Match an existing issue by title/problem overlap' has no defined threshold (semantic? keyword? percentage?), leaving the dedup decision — the most consequential branch — open to materially different interpretations across runs.
- `plugins/php-backend-sdlc/commands/sdlc-qa.md` J11 instruction-unambiguity = 3/5 — Step 2 says 'record the degrade note and finish with the degrade verdict' — 'degrade note' has no defined format and 'finish' is ambiguous: does it skip steps 3-5 entirely or just annotate the report before proceeding to assembly?
- `plugins/php-backend-sdlc/commands/sdlc-review.md` J11 instruction-unambiguity = 3/5 — The fr-nfr-reviewer Task-prompt parenthetical — 'instead of re-running the make.tests/make.e2e evidence targets the agent is permitted to run' — is ambiguous: it is unclear whether the agent MAY re-run those targets when stage-3 output is absent or MUST NOT re-run them regardless.
- `plugins/php-backend-sdlc/commands/sdlc-setup.md` J11 instruction-unambiguity = 3/5 — 'if a violation is a detection gap the user can fix in place' gives no criterion for distinguishing a retryable detection gap from a non-retryable structural violation, leaving the branching condition open to two materially different readings.
- `plugins/php-backend-sdlc/commands/sdlc.md` J11 instruction-unambiguity = 3/5 — 'title/problem overlap' for durable issue dedup has no matching criterion defined (fuzzy, substring, semantic?), and NFR-4 is cited in degrade_notes without being defined anywhere in the artifact, leaving two instructions open to materially different implementations.
- `plugins/php-backend-sdlc/skills/AI-AGENT-GUIDE.md` J11 instruction-unambiguity = 3/5 — 'Triage first (ADR-5/NFR-5)' in Step 0 uses unexplained acronyms with no definition in this file; a non-BMAD reader cannot determine whether these are rule codes, doc sections, or something else. Also, Example 2 says 'execute the body of each EXECUTE skill in order' without specifying what ordering criterion applies.
- `plugins/php-backend-sdlc/skills/api-platform-crud/SKILL.md` J6 profile-key-branching = 2/5 — persistence.engine is listed as a consumed key but never branched on in the body (appears only as a diagram label); quality.phpinsights.complexity and quality.infection_msi are listed but the body only hardcodes their floor values (94, 100) rather than branching on whatever the profile actually specifies.
- `plugins/php-backend-sdlc/skills/api-platform-crud/SKILL.md` J11 instruction-unambiguity = 3/5 — 'confirm against the installed major version' gives no concrete method; the checklist item 'Resource directory registered in API Platform package config' has no corresponding numbered step in the 10-step guide, creating an unanchored obligation.
- `plugins/php-backend-sdlc/skills/bmad-autonomous-planning/SKILL.md` J6 profile-key-branching = 2/5 — persistence.mapper/engine listed as consumed but only one branch illustrated (doctrine-odm/mongodb); framework.api_platform has only the 'when set' branch with no documented behavior for the false branch; architecture.shared_context listed but never branched on.
- `plugins/php-backend-sdlc/skills/bmad-autonomous-planning/SKILL.md` J11 instruction-unambiguity = 3/5 — Default validation-round count is unstated (inputs say 'optional ... 1 to 3' but no default given); 'prefer the matching BMAD validation command' leaves no criterion for when to deviate; 'only the minimum sources required' has no concrete selection rule.
- `plugins/php-backend-sdlc/skills/bmad-fr-nfr-review-gate/SKILL.md` J6 profile-key-branching = 2/5 — make.tests, make.e2e, make.infection, make.deptrac, and ci.required_checks are all listed under 'Profile keys consumed' but the body documents no null-branch behavior for any of them, leaving half the listed keys single-branched.
- `plugins/php-backend-sdlc/skills/bmad-fr-nfr-review-gate/SKILL.md` J11 instruction-unambiguity = 3/5 — Two ambiguities: step 6 'unbounded loop is acceptable only when intentionally chosen' names no actor or condition; step 5 'if manual testing is required' gives no trigger definition for when it is required vs optional.
- `plugins/php-backend-sdlc/skills/cache-management/SKILL.md` J6 profile-key-branching = 2/5 — framework.name is listed as consumed but only appears once as a vague aside ('per framework.name') with no concrete branching for any named framework; quality.phpinsights.complexity and quality.infection_msi are listed but only appear as inert floor constants, never as conditional branches.
- `plugins/php-backend-sdlc/skills/cache-management/SKILL.md` J11 instruction-unambiguity = 3/5 — The {Persistence} placeholder in services.yaml is not fully resolved: the naming section gives two possible conventions (plain vs engine-prefixed) with 'follow whichever convention the repository already uses' as the tie-breaker, but provides no algorithm to detect that convention, leaving the choice ambiguous.
- `plugins/php-backend-sdlc/skills/ci-workflow/SKILL.md` J6 profile-key-branching = 2/5 — All seven `quality.*` keys are listed as consumed but the body never branches behavior on their values — they appear only in a 'NEVER decrease' table with zero conditional paths (e.g., no 'if complexity floor is 94 vs 100, do X vs Y').
- `plugins/php-backend-sdlc/skills/ci-workflow/SKILL.md` J11 instruction-unambiguity = 3/5 — 'Repeat Steps 2-4 until it exits 0' has no defined behavior when fixes are impossible; 'check the Makefile before assuming they exist' for alternative commands leaves undefined what to do when absent; 'Organization | make.psalm target' mapping is confusing since psalm is static analysis, not an organization checker.
- `plugins/php-backend-sdlc/skills/clean-architecture-llm/SKILL.md` J6 profile-key-branching = 2/5 — architecture.shared_context has a partial branch ('when one exists' -> centralize there) but the missing-key branch is unspecified; source_root, bounded_contexts, make.ci, and make.tests are referenced as simple substitutions with no both-branch conditionals.
- `plugins/php-backend-sdlc/skills/clean-architecture-llm/SKILL.md` J11 instruction-unambiguity = 3/5 — 'get explicit approval before introducing a new Port/ directory' names no approver; 'when relevant' for docs updates provides no criterion; 'record prompt versioning' specifies neither mechanism nor location.
- `plugins/php-backend-sdlc/skills/code-review/SKILL.md` J6 profile-key-branching = 3/5 — make.ai_review_loop (non-null/null), make.pr_comments (non-null/null), and ci.provider (non-null/null) each have both branches; however review.coderabbit=true branch is undocumented (only false is described), review.request_changes_blocking false branch is only implied, and quality.* keys are listed as consumed but never branched on — only cited as floor values.
- `plugins/php-backend-sdlc/skills/code-review/SKILL.md` J11 instruction-unambiguity = 3/5 — Step 5 'repeat categorization and implementation' has no iteration cap so it can loop indefinitely if a reviewer keeps adding comments; 'compatible with repository rules' in Step 4 is undefined; ledger completion is spread across Steps 5, 7, and 8a with overlapping write instructions that could be applied in the wrong order.
- `plugins/php-backend-sdlc/skills/complexity-management/SKILL.md` J6 profile-key-branching = 2/5 — Only make.phpinsights gets a null branch; make.deptrac, make.tests, make.ci, and architecture.source_root are listed as consumed but used with zero conditional handling if absent, silently assuming they exist.
- `plugins/php-backend-sdlc/skills/complexity-management/SKILL.md` J11 instruction-unambiguity = 3/5 — 'note it and degrade' for a null make.phpinsights mapping is undefined — two materially different readings (skip entirely vs fall back to raw CLI) are both valid, and the five refactoring strategies in Step 4 have no selection rule between them.
- `plugins/php-backend-sdlc/skills/database-migrations/SKILL.md` J6 profile-key-branching = 3/5 — persistence.engine is listed as a consumed key and appears in the mapper table but the body never branches on mysql vs mariadb vs postgresql; framework.name only documents the symfony container branch with no else-path for non-symfony environments.
- `plugins/php-backend-sdlc/skills/database-migrations/SKILL.md` J11 instruction-unambiguity = 2/5 — Creating Repositories Step 2 shows '// Path A: EntityManagerInterface / Path B: DocumentManager' as comments but then unconditionally declares 'private readonly EntityManagerInterface $entityManager', leaving Path B implementers with broken code that injects the wrong manager class.
- `plugins/php-backend-sdlc/skills/deptrac-fixer/SKILL.md` J6 profile-key-branching = 3/5 — 'framework.name' is listed as a consumed key and appears in layer-rule prose as a label, but no conditional branch exists for different framework values (e.g., Symfony vs Laravel paths) — only one implicit behavior documented.
- `plugins/php-backend-sdlc/skills/documentation-creation/SKILL.md` J6 profile-key-branching = 3/5 — `persistence.mapper` is listed and appears in the checklist as 'ORM vs ODM described per mapper' but no dual-branch doc guidance exists (what differs in the docs for an ORM project vs an ODM project?); `persistence.engine` drives grep patterns but no engine-specific documentation branching.
- `plugins/php-backend-sdlc/skills/documentation-creation/SKILL.md` J11 instruction-unambiguity = 3/5 — 'Derive the remaining files from the structure in Format (Output) below' instructs the reader to infer templates for 15/17 files from a section that contains only a filename list, leaving structural content for those files undefined.
- `plugins/php-backend-sdlc/skills/documentation-sync/SKILL.md` J6 profile-key-branching = 2/5 — persistence.engine is listed and used only as a label ('names the engine in examples') with no absent-key path; architecture.bounded_contexts has no conditional at all; make.ci has no null branch; quality.phpinsights.complexity and quality.infection_msi are referenced as default values but never branched on; capabilities.structurizr false-branch is implicit, not documented.
- `plugins/php-backend-sdlc/skills/documentation-sync/SKILL.md` J11 instruction-unambiguity = 3/5 — 'when framework.api_platform is a version string' leaves the boolean-true case undefined; 'put content in the nearest equivalent' and 'covers the topic' lack criteria making them open to materially different reader interpretations.
- `plugins/php-backend-sdlc/skills/implementing-ddd-architecture/SKILL.md` J6 profile-key-branching = 3/5 — persistence.engine is listed as consumed but body only names its values; no actual behavioral branch (no 'if mysql → EXPLAIN ...' vs 'if mongodb → explain()'); framework.api_platform and framework.graphql each document only one branch (when-set), not the when-absent path.
- `plugins/php-backend-sdlc/skills/load-testing/SKILL.md` J6 profile-key-branching = 2/5 — persistence.engine, persistence.mapper, and architecture.bounded_contexts are listed as consumed keys but the body contains zero branches on any of them — no MySQL vs Mongo, no ORM vs ODM, no bounded-context routing.
- `plugins/php-backend-sdlc/skills/load-testing/SKILL.md` J11 instruction-unambiguity = 3/5 — make.start is listed as a profile key but no instruction branch exists for it (null vs non-null); the 'canonical K6 harness' block says 'find them in the Makefile' with no fallback if the harness is not canonical, leaving an unresolved conditional.
- `plugins/php-backend-sdlc/skills/observability-instrumentation/SKILL.md` J6 profile-key-branching = 3/5 — framework.name is listed as a consumed key and controls the logging stack, but the body documents only one branch (Symfony/Monolog channel + config/packages/monolog.yaml); no path for Laravel, Slim, or any non-Symfony framework is specified.
- `plugins/php-backend-sdlc/skills/observability-instrumentation/SKILL.md` J11 instruction-unambiguity = 3/5 — Generic-backend section lists Prometheus, StatsD, OTLP, and structured-log as options when observability_emf is false, but no profile key discriminates among them, leaving implementers without a deterministic selection rule.
- `plugins/php-backend-sdlc/skills/openapi-development/SKILL.md` J6 profile-key-branching = 2/5 — ci.workflows is listed as consumed but body only says 'check ci.workflows for a workflow'—no branch on present vs absent; make.phpinsights and quality.phpinsights.* keys also lack absent-value branches.
- `plugins/php-backend-sdlc/skills/openapi-development/SKILL.md` J11 instruction-unambiguity = 3/5 — 'the bounded context that owns API composition' is undefined and non-discoverable from profile keys alone; 'Note the gap if none exist' for make targets is ambiguous about the concrete action to take (report to user? block? skip?).
- `plugins/php-backend-sdlc/skills/quality-standards/SKILL.md` J11 instruction-unambiguity = 3/5 — 'Use the framework serializer' (Psalm ForbiddenCode row) leaves 'the framework' undefined; PHPUnit line coverage target is called 'an independent bar' with no profile key or lookup path, making the check unverifiable from the skill alone.
- `plugins/php-backend-sdlc/skills/query-performance-analysis/SKILL.md` J6 profile-key-branching = 3/5 — persistence.engine and persistence.mapper are fully branched; framework.name is listed and conditionally used ('When framework.name is symfony') but has no documented else branch — the non-Symfony case has zero guidance.
- `plugins/php-backend-sdlc/skills/query-performance-analysis/SKILL.md` J11 instruction-unambiguity = 3/5 — 'For postgresql, follow Path A's procedure conceptually with the tools above' is ambiguous — 'conceptually' leaves undefined which exact Path A steps apply versus which must be adapted, with no step-by-step clarification.
- `plugins/php-backend-sdlc/skills/structurizr-architecture-sync/SKILL.md` J6 profile-key-branching = 2/5 — persistence.engine is listed and used for naming but has zero behavioral branching (no MySQL-vs-MongoDB EXPLAIN or schema path); make.start is listed but never conditioned on; architecture.shared_context's non-null branch is 'documented only when architecturally significant' — undefined, not a real second branch.
- `plugins/php-backend-sdlc/skills/structurizr-architecture-sync/SKILL.md` J11 instruction-unambiguity = 3/5 — 'documented only when architecturally significant' appears twice for architecture.shared_context without defining significance, and 'appears in a Deptrac layer boundary discussion' presupposes Deptrac is configured — both create materially different valid readings.
- `plugins/php-backend-sdlc/skills/testing-workflow/SKILL.md` J6 profile-key-branching = 3/5 — `make.start` is listed as a consumed key but the body only mentions it in a bulleted prerequisite note under load testing, never branching on null vs non-null; the general null-skip rule does not substitute for per-key conditional branches.
- `plugins/php-backend-sdlc/skills/testing-workflow/SKILL.md` J11 instruction-unambiguity = 3/5 — 'add the target mapped by `make.e2e` for a comprehensive check before finishing' leaves 'before finishing' undefined (before every commit? only before PR merge?), and 'Coverage below target' in the debug table never cites which profile key holds that target.
